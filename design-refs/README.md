# design-refs

Design reference material that should live in the repo but must NOT ship in
the deploy. Anything under `public/` is copied verbatim into every Vercel
build and is publicly browsable on coco36.com — screenshots, handoff notes,
and pasted uploads belong here instead.

- `hero-handoff/` — screenshots + working files from the July 2026 hero
  redesign handoff (moved out of `public/hero-handoff/`).
